#!/usr/bin/node

console.log(process.argv[2] + ' is ' + process.argv[3]);

/* you can also concartenate in JS by using the "$"
 * it is enclosed within a backtick "``"
 * console.log(`${process.argv[2]} 'is' ${process.argv[3]}`);
 */
